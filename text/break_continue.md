# Break and Continue
Two special statements are available inside `while` and `for` loops:

`break` exits the loop, even it occurs partway through a pass through the loop.

`continue` abandons the current pass through the loop, but returns to the top to check the condition again (in a `while` loop) or go on to the next element of the sequence (in a `for` loop).